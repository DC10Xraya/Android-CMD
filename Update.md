**2026-09-13 / 0.21.1**
[修复] RUNNING中帮助菜单cecho误打为ececho问题导致common not found的问题
[修复] FUN帮助菜单中的--remove与条件判断不一致(条件判断中要求--remove-all)的问题, 现在移除全部自定义函数使用--remove/-r
[优化] 如果用户输入的命令既不在case, 也不是fun自己定义的, lazy_load加载也失败, 那么最后如果输入的这个命令在系统中存在, 就调用系统命令
[新命令] BASH命令直接调用bash程序(跟C一样也包装, 但是和C有些不同)

**2026-09-12 / 0.21**
[新命令] EXTS: 列出可执行文件, 无目录时扫描 $PATH
```exts_args
-e, --ext             只显示扩展目录(排除 /system 等系统目录)
-d, --dirs            只显示目录路径, 不列出文件
-p, --path            显示完整路径
-c, --count           只显示数量统计
-s, --search <关键词>  过滤包含关键词的命令
-h, --help            显示帮助
```
[新功能] CMD在无参数时将进入系统交互(系统shell), 仍然可以直接接命令来执行单个命令
[新功能] FUN现在会提示用户是否覆盖已定义的脚本函数, 并且增加了管理:
```fun
-l        列出用户已定义函数
-d <名称>  删除指定用户函数
-r        删除全部用户函数
**WARN: 覆盖了系统的函数之后是不可逆的, 除非重新启动, 覆盖cmd_fun将不可管理, 覆盖一些重要脚本函数之后可能会出现一些不可预测的问题(这些是你自己的选择)**
```
[新功能] 自定义解析器beta3现在会展开自定义变量: $pwd / $sdir / $self / $0 (支持转义)
[新功能] Uptime现在支持系统的选项并且自动汉化, 负载数字根据当前数字和CPU核心数标上绿色/黄色/红色的颜色
```uptime
用法: UPTIME [-p|-s] [-v]
(无参数)   默认格式, 已汉化
-p         系统美化输出(human readable), 已汉化
-s         显示系统启动时间, 已汉化
-v         输出原始输出(不汉化)
```
```uptime_eg
u0_a420-> uptime

  自10:14:12 已经运行13天, 23时52分, 0个用户
  最近1分钟, 5分钟, 10分钟的过载: 7.46, 8.80, 7.36
u0_a420-> uptime -v

 10:14:22 up 13 days, 23:52,  0 users,  load average: 6.70, 8.59, 7.31
**TIP:不是这种格式的会原样输出**
```
[新功能] DNS现在支持更多选项, 输出时会显示采用的哪种解析、哪种工具
```dns_new_args
解析方向:
默认根据目标自动判断(IPv4→反向, 域名→正向)
-0                强制正向解析(域名 → IPv4)
-1                强制反向解析(IPv4 → 域名)

输出控制:
-q                安静模式, 只输出结果(不显示方向/来源)
-v                显示系统命令/API 的原始输出

解析渠道(选一, 不可同时使用):
-n                强制走网络API(跳过所有本地工具)
-w <0/1/URL>      指定网络API (自动隐含 -n)
                    0     = 阿里云API (默认)
                    1     = Cloudflare DoH
                    其他  = 自定义 URL
--nslookup        仅使用 nslookup
--host            仅使用 host
--dig             仅使用 dig
--getent          仅使用 getent
--busybox         仅使用 busybox nslookup
--ping            仅使用 ping (仅正向)

其它:
-t, --test        列出当前可用的本地/网络工具
```
[新功能] CECHO(包括内部文字输出和用户命令)以及底层输出_cprint现在可以使用-c256/-cb256来使用256颜色(不能与-c或者-cb同时使用)
[修复] TEMP太慢问题(改用bash内建, 不再用几十个fork)
[优化] ulimit -u将会在脚本退出时恢复
[微调] 现在标题不会显示提示用引号包裹(你们应该都知道); INFO同时用作用户命令和打开代码时查看信息; 推荐终端宽度为65个字符
[???] 输入你自己的用户名和911会得到"神秘东西"

**2026-09-05 / 0.20**
[大改动] 移除了主脚本中复杂的信号处理逻辑, 现在主脚本只管自己, 其他命令依靠他们自己的陷阱, 而不是通过运行变量通知主脚本
[新命令] awk grep sed cut 这四个命令的简单包装
[修复] CONFIG加载太慢, 现在使用更简单的bash内置命令, 并且简化了加载步骤, 通过config指令可以更好控制
```config
-h, --help     显示此帮助
-c, --clean    清除配置文件并重置为默认值(文件将被删除)
-r, --reset    重置所有配置为默认值(覆盖配置文件)
无参数          显示当前配置内容
```
[修复] 资源目录中AWKC/BC实际内容与命名相反, 导致无法正常使用这两个命令的问题
[修复] 现在ZIPDIFF退出时会保存回原来的陷阱, 防止主程序退出逻辑被篡改
[优化] 现在最多只能同时加载(set)20个外部(资源目录)命令, 超过将会从最旧的开始卸载, 通过resource命令可以更好的控制
```resource
ls                  列出所有可用的资源文件
laod                列出当前已加载的资源函数
del                 卸载全部已加载资源
del <N>/<函数名>    卸载最旧的 N 个(正整数)/卸载指定函数
-h, --help, help    显示帮助
 ```
[优化] 现在DNS除了阿里云API同时支持Cloudflare DoH作为备选(默认最后一个使用)
[优化] 主程序会自动检查tmp目录大小, 大于1MB时提醒, 同时更新了TMPDIR
```tmpdir
-h, --help     显示此帮助
-c, --clean    清除临时目录中的所有内容
-s, --set <目录> 设置临时目录
无参数          显示当前临时目录
```
[微调] 如你所见, 这样的更新日志格式将在以后继续使用
[微调] 现在MC的两个命令不会显示到帮助菜单中, 重新对齐了帮助菜单
[???] 增加了一个沃尔玛命令(walmart)

**2026-08-29 / 0.15.1(dev249)**
1. 增加了ZIPDIFF, 比较ZIP差异, 并输出新增/修改/移除/不变的文件
2. UPDATE命令和自动更新功能现支持0.**.*这样的版本号
3. resource中一些原来在other的文件移到了psd
4. 修复了case中部分命令无"${args_array[@]}"导致不传入参数问题

**2026-08-28 / 0.15(dev247)**
1. 增加:
DUMP    -扁平化文件夹结构(所有内容导出到TXT)
FUN/FUNCTION  -临时定义函数
JSON          -检验JSON/JSON5(JQ/PY/BASH)
CAT           -系统的cat(查看文本)
HEAD/TAIL     -显示文件开头或结尾N行, 支持系统选项
DD            -复制并转换文件(系统)
FORMAT        -格式化存储(包装)
MOUNT         -挂载文件系统
UMOUNT        -卸载文件系统
2. 移除了C的炸弹检测, FORK BOMB被移除
3. TASKMAG现在支持^C退出, 原来的ESC仍然生效

**2026-08-24 / 0.14(dev241)**
1. 增加:
CODEWC  -统计纯文本编程语言的有效行数、单词数和字符数(自动忽略注释和空行)(BETA)
- 支持语言：Shell、Python、C/C++/Java/JS/Go/Rust、HTML、XML、Lua、SQL、YAML、Make、Conf
CD      -更改工作目录(本来以前有, 不知道什么时候删掉的)
2. PATH改为输出PATH变量, 脚本所在目录命令改为SDIR
3. 修复了WC没有任何参数时卡住的问题(改为显示帮助)
4. 再度修复DISKT/DISKC遗留临时文件问题
5. 主循环现在只显式特殊命令映射, 其余的正常命令全部靠lazy_load

**2026-08-23-A / 0.13(dev238)**
1. 增加:
- WC         -统计行数、单词数、字符数(系统WC,美化输出)
- DIFF       -比较两个文件/目录差异
- CMDTIME    -测量命令执行耗时(系统time)
- CPUMONITOR -实时显示每个CPU核心频率
2. sysinfo现在会显示CPU核心频率和详细的内存信息
3. batt显示更详细

**2026-08-23 / 0.12(dev234)**
1. tree增加了-a选项(显示文件夹的大小), 并且修改了颜色显示, 增加了防止无限递归
2. 增加了MCMODDOWNLOAD可以从modrinth上下载MC JAVA版的模组
3. 现在强制要求wget或者curl中的其中一个

**2026-08-22 / 0.11(dev230)**
1. 增加了ulimit命令, 类似logcat的做法
2. 在脚本运行一开始就设置1024的进程限制
3. 移除了calc, 将bc、awkc挪入资源目录
4. 随机标语用MCJE逻辑, CECHO标准
5. ccat现在可以输出文件
```ccat
ccat "update.md" << "EOF" #支持相对路径, 使用脚本自身路径拼接(也支持绝对路径)
EOF
```
6. 修复了UPDATE的一些问题, 并说明发布时间为UTC

**2026-08-18 / 0.10(dev225)**
#这次更新太疯狂, 我就直接到0.10了:D
1. 自定义解析器(BETA)替代IFS分割
(1). 单引号内的所有字符全部作为一个参数
(2). 双引号内的空格保留, 支持反斜杠转义(如 \"、\\ 等)
(3). 反斜杠转义 \：可用于转义空格、#、引号等特殊字符
(4). 注释：行内未被引号包裹的 # 之后的所有内容均被忽略
(5). 无引号分割：以空格和制表符作为函数分隔符, 多个连续空白视为一个
(6). 处理空输入：如果只按回车或输入仅含注释的无意义内容, 则不换行
*(目前除了主程序, 资源目录中的命令仅同步更新FTP为自定义解析器)
```多个空格用引号包裹
u0_a0-> cp "/storage/emulated/0/   file.txt" "/sdcard"
```
```注释和转义
u0_a0-> ping localhost #测试
正在Ping localhost...

u0_a0-> ping \#参数
正在 Ping #参数...
```
```引号内注释忽略
u0_a0-> echo "文字内注释#"
文字内注释#
```
2. 修复了CAL 0不输出的问题并且优化了CAL
3. 新命令
(1). netneig -扫描局域网下的设备(复用scan指令)
(2). DNS [IPV4/域名]  -相互转换IPV4或域名
(3). config  -配置文件, -r恢复默认, 其他的均为显示
(4). logcat  -直接调用logcat, 附加中文帮助菜单
(5). update  -显示调用检查更新
(6). wm      -窗口信息(res为兼容仍保留)
(7). diskt   -检测当前存储设备的顺序和随机读写速度(不精确, 各个挂载点测试命令为DISKC)
(8). info    -脚本自身信息
4. 新功能
(1). 现在脚本会自动从Github检测最新版本并提示更新, 通过update命令查看详情
(2). tree现在支持三个选项:
-d          只显示目录
-L <深度>   限制递归深度
-s          显示文件大小
(3). FTP连接新用法:(原来的网址用法仍然生效)
```ftp
FTP connect <主机> [端口(默认21)] [用户名(默认anonymous)] [密码(可空)] [初始路径(可空)]  #必须按照顺序
```
(4). Portscan照抄了DNS命令中的域名解析为IP
(5). ccat批量输出文字并且保持颜色的支持(便利开发, 对使用者无感)
```ccat
ccat << EOF #带引号不展开,不带引号展开
我是文本，默认颜色
//cecho -b -c 31 我也是文本，但是是特殊样式
$我是变量, 不带引号时会被展开
EOF
```
(6). 现在CLS -y 相当于 -r
5. 编码和解码
(1). 编码和解码不再是实验性内容(经过验证可用)
(2). 增加了sha256, 为md5增加了警告
(3). sha1的优化
(4). 移除了url编码、解码(这应该是py干的事)
6. 增加了任务管理器运行时间项, 移除了3秒钟等待
7. 帮助菜单的改造, 帮助菜单更简洁
8. 修改了大量命令, 为他们添加了-h/--help
9. 小修改
(1). scan/portscan默认线程由4改为8
(2). systeminfo新增别名sysinfo
(3). 移除了TMPDIR about分支
(4). 资源目录下的文件夹名不再有特殊字符(如0A网络→net)
(5).root提示符由红改为紫 ;标题有一些改动 ; 略微重构了结构, 一些注释被重写

**2026-08-08-A / 0.06(dev187)**
1. 增加历史命令功能, 通过上下箭头调用历史命令, 通过HISTORY命令管理
2. 增加CLSD命令 控制CLS默认是否显示标题, CLS本身可以通过参数指定模式, CLSNT被CLS -n替代
3. SH命令移除了炸弹检测(运行自己都说自己是炸弹), C命令仍然保留并添加了--force参数
4. 配置和历史命令现在在/#主程序目录/etc目录下
5. 重构了前部分的结构、其它小优化

**2026-08-08 / 0.05(dev183)**
-. 实现懒惰加载, 现在资源目录的bash只会在用户执行的时候加载, 而不是一次性全部加载(速度可能没有明显提升,但总之提高了效率)
TIP:如果你执行所有的命令, 那么所有的都会被加载到内存里(>=0.20的版本最多只能加载20个)
[最后一次修订于2026-09-05]

**2026-08-02 / 0.04(dev178)**
1. 增加了HACK2指令:穷举可打印字符直到找到目标, 但是使用二分法
2. 优化了运行前检查, 重构了整个结构, 并添加了莫名其妙的更新

**2026-07-31 / 0.03(dev170)**
1. COLOR、TITLE、TMPDIR 的设置现在会保存到配置文件 resource/.cmd_config
2. 将所有ESC统一改为Ctrl+C
3. HACK现在支持Ctrl+C停止, BC支持Ctrl+C停止正在进行的运算
4. 其他修改

**2026-07-30 / 0.01(dev163)->0.02(dev166)**
1. 增加了HACK指令:穷举可打印字符直到找到目标
2. 修改了TMPDIR的行为:现在回退目录是/#脚本所在目录/tmp, 原来的/storage/emulated/0/tmp作为最后回退
3. 其他的略微修改, 以及新的版本号格式